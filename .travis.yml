os: osx
language: generic
osx_image: xcode9.4
before_script:
  - brew update
  - brew install yasm
script:
  - ./init-ios.sh arm64
  - cd ios
  - ./compile-ffmpeg.sh clean
  - ./compile-ffmpeg.sh arm64
  - git status
