os: osx
language: generic
osx_image: xcode9.4
env:
  -OUTPUT_DIR="IJKMediaPlayer/IJKMediaFramework.framework"
  -PARENT="1BPIKK7sxbfdDu59wKmp3dl83lXpTkHRJ"
before_script:
  - brew update
  - brew install gdrive
  - brew install yasm
script:
  - ./travis-run.sh 
  - gdrive upload --recursive --refresh-token ${REFRESH_TOKEN} --parent ${PARENT} ${OUTPUT_DIR}   
